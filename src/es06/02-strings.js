let hello = 'hello';
let world = 'world';
let epicPhrase = hello + ' ' + world + '!';

///Template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

//Multi-line strings
let lorem = 'There i a string \n' + 'this is another line';

let lorem2 = `There is a epic phrase
this is the next phrase!`

console.log(lorem);
console.log(lorem2);